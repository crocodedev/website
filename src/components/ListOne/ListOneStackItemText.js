import styled from "@emotion/styled";
import Text from "../Text";

const ListOneStackItemText = styled(Text)`
  flex: content;
  max-width: 380px;
  text-align: center;
`;

export default ListOneStackItemText;
