import styled from "@emotion/styled";
import SectionWrapper from "../SectionWrapper";

const ArticleWrapper = styled(SectionWrapper)`
  max-width: 846px;
  width: 100%;
  margin-left: auto;
  margin-right: auto;
`;

export default ArticleWrapper;
