import styled from "@emotion/styled";

const HeaderMenu = styled.label`
  align-items: center;
  cursor: pointer;
  display: flex;
  padding: 10px;
  position: relative;
  transition-duration: 1s;
`;

export default HeaderMenu;
