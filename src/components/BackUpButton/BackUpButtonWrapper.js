import styled from "@emotion/styled";

const BackUpButtonWrapper = styled.div`
  width: 100%;
  height: 100%;
  position: relative;

  html {
    scroll-behavior: smooth;
  }
`;

export default BackUpButtonWrapper;
