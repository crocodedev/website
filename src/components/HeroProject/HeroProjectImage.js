import styled from "@emotion/styled";
import Image from "../Image";

const HeroProjectImage = styled(Image)`
  width: 100%;
  overflow: hidden;

  width: 100%;
  height: 100%;
  object-fit: cover;
`;

export default HeroProjectImage;
