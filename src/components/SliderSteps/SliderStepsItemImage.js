import styled from "@emotion/styled";

const SliderStepsItemImage = styled.div`
  width: 100%;
  overflow: hidden;

  picture img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
`;

export default SliderStepsItemImage;
