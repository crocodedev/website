import styled from "@emotion/styled";
import Text from "../Text";

const CardHeadingTitle = styled(Text)`
  white-space: nowrap;
  text-overflow: ellipsis;
  overflow: hidden;
  width: 100%;
`;

export default CardHeadingTitle;
